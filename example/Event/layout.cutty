
// Document Metadata
metadata {
    title : "Cutty - Event Example",
}

// Document Viewport
viewport {
    button {
        click : event("Click!"),
        hover : event("Hover!"),
        blur : event("Blur!"),
        content : "Button"
    }

    input {
        type : text,
        hint : "Input Text",
        focus : event("Focus!"),
        change : event("Change!"),
        keydown : event("KeyDown!"),
        keyup : event("KeyUp!")
    }
}

// Document Script
script {
    function event(event_type) {
        console.log("Event : " + event_type);
    }
}